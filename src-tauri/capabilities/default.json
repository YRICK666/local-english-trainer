{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capability for the main static window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}